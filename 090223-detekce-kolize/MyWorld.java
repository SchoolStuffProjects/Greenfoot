import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class MyWorld here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class MyWorld extends World
{

    /**
     * Constructor for objects of class MyWorld.
     * 
     */
    public MyWorld()
    {    
        // Create a new world with 600x400 cells with a cell size of 1x1 pixels.
        super(600, 400, 1); 
        prepare();
    }

    /**
     * Prepare the world for the start of the program.
     * That is: create the initial objects and add them to the world.
     */
    private void prepare()
    {
        /*Zed zed = new Zed();
        addObject(zed,208,93);
        Zed zed2 = new Zed();
        addObject(zed2,439,246);
        Zed zed3 = new Zed();
        addObject(zed3,124,302);
        Zed zed4 = new Zed();
        addObject(zed4,447,75);
        Micek micek = new Micek();
        addObject(micek,300,200);*/
        
        /*Panacek panacek = new Panacek();
        addObject(panacek,300,200);
        Kamen kamen = new Kamen();
        addObject(kamen,500,140);*/
        
    }
}
